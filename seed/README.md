# Seed data

Seed data is deterministic and safe to recreate in development and tests. It must never contain production credentials or secrets. B0 will add the organization, station, role assignments, JWT key metadata, and audit chain lock required by the integration environment.
